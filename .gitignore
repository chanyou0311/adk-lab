# Python
__pycache__/
*.py[cod]
*.egg-info/
.ruff_cache/

# venv
.venv/

# secrets — 実際の資格情報は commit しない
.env

# NOTE: fixtures 生成物 (src/lab/fixtures/) と eval/results/*.json は意図的に commit する
# (実験の再現性・ブログでの引用のため)。ここでは除外しない。
